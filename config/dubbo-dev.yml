# 用户无需改动
dubbo:
  application:
    name: ${spring.application.name}-v${spring.version}
  protocol:
    name: dubbo
    port: -1
  registry:
    address: nacos://${nacos.host}:${nacos.port}
    timeout: 60000
  consumer:
    check: false