spring:
  version: @project.version@
  application:
    name: @project.artifactId@
    des: "Sonic云真机测试平台 - 文件中心"
  profiles:
    active: composite, ${PROFILE:dev}
  cloud:
    config:
      name: @project.artifactId@,actuator,zookeeper,feign,logging,knife4j,user
      server:
        composite:
          - type: ${CONFIG_TYPE:native}
        bootstrap: true